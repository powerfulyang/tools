import React from "react";
import { PhotoSlider } from "@powerfulyang/components";

export const PhotoView = () => {
  return <PhotoSlider />;
};

export default {
  title: "photo-view",
  component: PhotoView,
};
