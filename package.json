{
  "name": "root",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "bootstrap": "pnpm install --shamefully-hoist",
    "clean": "lerna clean -y && rm -rf node_modules",
    "build": "lerna exec -- npm run build",
    "lint": "lerna exec -- npm run lint:js"
  },
  "devDependencies": {
    "lerna": "5.4.0"
  },
  "workspaces": [
    "packages/*",
    "project/*"
  ]
}
