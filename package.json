{
  "name": "root",
  "private": true,
  "devDependencies": {
    "husky": "^4.3.0"
  },
  "workspaces": [
    "packages/*",
    "project/*"
  ],
  "scripts": {
    "precommit": "npm run prettier && npm run lint && npm run fix:components:style"
  },
  "husky": {
    "hooks": {
      "pre-commit": "npm run precommit"
    }
  }
}
